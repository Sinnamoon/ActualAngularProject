import { GetArticlePipe } from './get-article.pipe';

describe('GetArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new GetArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
